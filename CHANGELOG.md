# Changelog

Each Viva module is versioned and released on its own, so each package keeps its own changelog:

- [Viva Core](Packages/com.vivagames.core/CHANGELOG.md), releases tagged `core/vX.Y.Z`.
- [Viva Analytics](Packages/com.vivagames.analytics/CHANGELOG.md), releases tagged `analytics/vX.Y.Z`.

Changes to the development project itself (this repository outside `Packages/`) are described in the commit history.
